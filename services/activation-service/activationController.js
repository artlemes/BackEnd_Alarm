const axios = require('axios')

const activateAnAlarm(req, res) async => {
    const { alarmId, userID } = req.body //ver se é o usuário com permissão
}

const deActivateAnAlarm(req, res) async => {
    const { alarmId, userID } = req.body
}